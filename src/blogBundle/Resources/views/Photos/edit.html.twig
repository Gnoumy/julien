{% extends '::admin.html.twig' %}

{% block body -%}

	<div class="container">
		<div class="row">
			<h1>Modifications</h1>

		    {{ form(edit_form) }}

	        <ul class="record_actions">
			    <li>
			        <a class="btn btn-primary"href="{{ path('pagePhoto') }}">Retour à la liste</a>
			    </li>
		    	<li>{{ form(delete_form) }}</li>
			</ul>
		</div>
	</div>
    
{% endblock %}
