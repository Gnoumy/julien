{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Photos creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('pagePhoto') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
