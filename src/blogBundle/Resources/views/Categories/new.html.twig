{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Categories creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('pageCategorie') }}">
            Retour à la liste
        </a>
    </li>
</ul>
{% endblock %}
